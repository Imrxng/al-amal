export interface FormData  {
    name: string;
    surname: string;
    phone: string;
    email: string;
    type: string;
    messageText: string;
  };
  
  export interface Gebed {
    naam: string;
    tijd: string;
}